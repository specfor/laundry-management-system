export function useAddNewModel() {
    const show = ref(false);
    const title = ref('');
    const fields = ref([]);
    const modelSuccessButtonText = ref('');
    const modelOnClose = ref(null);
}